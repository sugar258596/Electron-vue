import { withInstall } from '@/utils'

import basicVideo from './BasicVideo.vue'
import basicTitle from './BasicTitle.vue'

export const BasicVideo = withInstall(basicVideo)
export const BasicTitle = withInstall(basicTitle)
