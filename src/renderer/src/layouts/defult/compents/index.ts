import resetLayouts from './src/ResetLayouts.vue'
import backTop from './src/BackTop.vue'

import { withInstall } from '@/utils'

export const ResetLayouts = withInstall(resetLayouts)

export const BackTop = withInstall(backTop)
